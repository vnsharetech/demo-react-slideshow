const initState = {
  title: "What We Do",
  content:
    "We develop successful apps for our clients who range from startup entrepreurs to Fortune 500s",
  cards: [],
};
function cardReducer(state = initState, action) {
  return state;
}
export default cardReducer;
